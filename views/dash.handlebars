  {{> main-open}}

<h1>Dashboard</h1>
<section id="currentRates">
{{#each rates}}
    <section class="card">
        <a href="/rates/{{id}}" class="card-body">
            <h3 class="card-title">{{title}}</h3> 
            <h5>posted{{createdAt}}</h5>
            <p class="card-text">{{content}}</p>
        </a>
    </section>
{{/each}}
</section>
<section id="createNew" class="card text-center">
    <div class="card-body">
    <h3 class="card-title">rate my ta</h3>
    <form id="newRate">
    <div class="card-text"></div>
      <label for="title">title</label><br>
      <input type="text" id="title" placeholder="title">
    </div>
        <div class="card-text"></div>
        <label for="content">content</label>
        <textarea id="content" placeholder="content"></textarea>
        </div>
    <button id="create">create</button>
    </form>
    </div>
</section>
    <form id="newPost">
        <button>post</button>
    </form>

<h1></h1>
    <section class="card">
        <a href="/rates/{{hid}}" class="card-body">
        <h3 class="card-title">{{title}}</h3> 
        <h4>posted by {{user.username}}</h4>
        <p class="card-text">{{content}}</p>
        </a>
    </section>

  {{> main-close}}

{{> script-dashboard}}